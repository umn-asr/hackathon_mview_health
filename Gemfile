git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

source "https://artifactory.umn.edu/artifactory/api/gems/asr-rubygems" do
  # Specify your gem's dependencies in degree_progress.gemspec
  gemspec
end
